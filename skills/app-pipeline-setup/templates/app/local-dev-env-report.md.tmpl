# Local Development Environment — ___APP_NAME___

**⚠️ Contains real credentials.** Do not commit this file to git, paste it
into a public/shared channel, or leave it lying around after the team has
what they need. Share it via a secure channel (password manager, direct
message) — the same handling as any other secret.

Generated by the `app-pipeline-setup` skill so developers can run
`___APP_NAME___` against real shared resources (S3, database) from their
own machine, without needing SSM/SSH access to the deploy box itself.

## `.env` for local development

Copy these into your local `.env` (values marked "local only" differ from
what's on the deploy box — use the local value, not the production one):

```
___LOCAL_ENV_BLOCK___
```

## Notes

- **Database**: ___DB_LOCAL_NOTE___
- **S3**: ___S3_LOCAL_NOTE___
- **App URL**: point anything that calls this app at
  `http://localhost:___PORT___` locally, not the production domain —
  hardcoding the prod URL here is a common source of "works on my machine
  but hits prod" bugs.

## Regenerating this file

This is a point-in-time snapshot. If credentials rotate (S3 key rotated,
RDS password changed) this file goes stale — regenerate it rather than
hand-editing, so it stays a trustworthy source of truth.
